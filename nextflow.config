params {

cellranger_software_path =   "path/to/cellrangersoftware/"

cellrangers_outdir       =   "${baseDir}/results/cellrangersouts/"

refdir                   =   "path/to/refgenome/"

fastq_path               =   "path/to/fastq_files/"

ID                       =   "ID/for/cellranger count software"

experiment_id            =   ""

scrublet_PREFIX          = "${baseDir}/results/cellrangersouts/"

scrublet_SUFFIX	         = "/outs/filtered_feature_bc_matrix"

scrublet_OUT             = "${baseDir}/results/scrubletdir/"

scrublet_dir             = ''

in_key                   = ""

in_samples               =

outdir                   = "${baseDir}/results"

qc_in_dir                = "${baseDir}/results/"

qc_output                = "${baseDir}/results/qc"

// Generic Options
publish_dir_mode = 'copy'


// Defaults only, expecting to be overwritten
max_memory = 350.GB
max_cpus = 64
max_time = 240.h

}



